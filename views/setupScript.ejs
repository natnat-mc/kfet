<script>
<% if(session) { %>
	const session=JSON.parse(atob("<%- Buffer.from(JSON.stringify(session), 'utf8').toString('base64') %>"));
<% } else { %>
	const session=undefined;
<% } %>
(() => {
	const scr=document.createElement('script');
	scr.setAttribute('src', '/res/script.js');
	window.onload=() => {
		document.head.appendChild(scr);
	};
})();
</script>
